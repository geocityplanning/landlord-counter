# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# 密钥与本地配置（勿提交）
.env
*.env
!.env.example
node_modules

# 牌局运行时数据(事件日志, 不入库)
data/games/*.jsonl
data/games/*.csv

# 计量/计费运行时数据(绝不可入库)
data/usage/
data/

# 录屏帧(自用, 不入库)
data/rec/
data/_polluted/
!data/calib/
# 标定截图存档(牌局画面属敏感数据 ⇒ 绝不入库 ✗; 用户 2026-09-18 要求保留供复核 ✓)
data/calib/shots/

# 伴随应用本地库(牌局数据敏感, 绝不进公开仓 ✓)
data/companion.db*

# Android 构建产物 + 试用签名(不进公开仓 ✓)
android/build/
android/debug.keystore

# 游戏资源由 tools/build_apk.sh 在打包时拷进 build/assets(唯一源码 = lab/guandan_www)
android/assets/
