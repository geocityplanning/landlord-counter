# M4 掼蛋进展（截至 2026-09-11）

## 一、当前能力

| 能力 | 状态 |
|---|---|
| 自动进局（重开浏览器 → 点"开始游戏"） | ✅ |
| 托管出牌（提示驱动 / 自研决策双模式） | ✅ |
| 整局打完 + 自动续局 | ✅ |
| 自研决策（rules+ai 移植，75 项测试 + 500 局属性测试） | ✅ 实机出牌 |
| 结算统计（VLM 读弹窗 → CSV） | ✅ |
| 夜间长跑（窗口制 + 看门狗自愈） | ✅ 已验证 |
| A/B 对照（自研 vs 提示） | ✅ 已就绪 |

## 二、首次夜跑结果（2026-09-10 21:00 ~ 09-11 03:00）

- **有效局数 81 局**，我方升级 32 / 对手升级 49 → **我方头游率 40%**
- 6 个窗口中 **3 个出数据**（23 点、1 点、2 点）
- 失败窗口原因：浏览器页面重开后偶发"能截屏但不吃点击/JS 未就绪"，循环未识破 → 整窗空转
- 数据归档：`/tmp/guandan_stats_20260910.csv`（原始 147 行 = 每局 2 行，含换行）

## 三、已修复（2026-09-11）

1. **prep v2**（`tools/guandan_prep.py`）：重开后校验页面是否真的可用（大金钮 / 对手牌 / 选中态），不可用则重开，最多 3 轮
2. **看门狗自愈**（`guandan/agent.py`）：4 分钟无任何进展（无按钮、无我方回合动作、手牌白卡无变化）→ 自动重开页面续跑；实测 0 误触发
3. **自研执行层加固**：读牌失败重新取帧重读一次；跟牌走提示执行时放宽张数校验（>0 张即出，我们只判出/不出）
4. **统计打标签**：CSV 增加模式列（`ours` / `mvp`），用于 A/B 对比

## 四、A/B 对照实验设计

- 每晚 21/22/23/0/1/2 六个 55 分钟窗口，**奇数点跑自研决策、偶数点跑提示驱动**
- 每局结算由 VLM 判定"我方是否升级" → 写 CSV（含模式标签）
- 明早对比两组"我方升级率"，决定后续投入方向：
  - 自研更高 → 优先修自研执行层（选牌稳定性）
  - 提示更高 → 自研侧重"出/不出"判断，选牌继续借提示
  - 接近 → 继续加数据量（多晚）

## 五、下一步（候选）

1. M4e 策略增强：队友配合（让牌/接风）、领出选择、关键手留主牌
2. 演示素材：录屏一段"无人值守连打 5 局"（M5 厂商演示包雏形）
3. 多晚数据积累后再定"提示 vs 自研"的最终形态
